{
  "name": "@artico/server",
  "version": "0.1.0",
  "description": "artico signalling server",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf ./build",
    "dev": "ts-node src",
    "dev:watch": "nodemon src -e ts,json --exec 'yarn run dev'",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.5",
    "nodemon": "^2.0.22",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "tsconfig": "*",
    "typescript": "^5.1.3"
  }
}
